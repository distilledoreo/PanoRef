# Optional local overrides for MCP workspace + rendering.
# Copy to mcp-server/.env.local if you want custom paths.

CONTINUITY_WORKSPACE=C:\Users\disti\Documents\ContinuityProjects
CONTINUITY_STAGE_URL=http://127.0.0.1:3000
CHROME_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe

# --- ChatGPT Plus (recommended): Cloudflare quick tunnel ---
# Terminal 1: npm run mcp:http
# Terminal 2: npm run mcp:cloudflare   (prints https://....trycloudflare.com)
# Terminal 3: npm run dev              (only for continuity_render_storyboard)
# ChatGPT connector: Connection type URL -> https://<host>/mcp, No Authentication

# --- OpenAI Secure MCP Tunnel (Codex / API / Business workspace) ---
# CONTROL_PLANE_API_KEY=sk-proj-...
# CONTROL_PLANE_TUNNEL_ID=tunnel_0123456789abcdef0123456789abcdef
# Setup: npm run mcp:tunnel:setup && npm run mcp:tunnel